### VAL-CROSS-015: New graph nodes reachable via graph navigation
Every new graph node added during the content-expansion milestone (macro thesis, use cases, competitors, community — or whichever ~4 nodes bring the total to ~13) is navigable via the `GraphShell` interactive mindmap, not only by direct URL hash (`#graph-<id>`). A user starting at the default graph workspace (`DEFAULT_GRAPH_NODE`) must be able to reach each new node through at most two clicks on graph-node or related-node controls.
Evidence: Render `GraphShell` at the default state. For each new node ID added to `GRAPH_NODES`, confirm a rendered interactive element (node button or related-node link) eventually leads to `focusedNodeId === newNodeId`. Verify `getGraphNodeById(newNodeId)` returns a valid `GraphNode` and that `getRelatedGraphNodes(newNodeId).length >= 1` (every new node has at least one bidirectional edge). Direct URL-only reachability without a graph-navigation path is a fail.

### VAL-CROSS-016: Investor pitch path references or includes new macro-thesis content
The investor pitch path (`INVESTOR_PITCH_GATES`) must reference or incorporate new content surfaces so that the guided VC-friendly narrative flows logically from the market/macro thesis into BETTER's specific solution. Specifically, the "Problem" gate (`gate-problem`) or a new gate must expose macro-thesis context (market size, fragmentation thesis, regulatory tailwinds) before the "Wedge" gate. If a dedicated macro-thesis graph node exists, the pitch path must include or link to it.
Evidence: Inspect `INVESTOR_PITCH_GATES` ordering. Confirm the gate immediately before `gate-wedge` covers macro/market thesis content (check its `description` and corresponding surface rendered content). If new graph nodes are added to the pitch path, confirm `TOTAL_GATES` increments accordingly and `getGateIndexForNode` returns valid indices for new node IDs. Fail if the pitch path skips directly from brand identity to live product with no market-context bridge.

### VAL-CROSS-017: Seamless navigation between old and new surfaces
Transitioning between existing surfaces (roadmap, tokenomics, architecture, etc.) and any newly added surfaces must feel visually seamless. All surfaces must use the same card component (`LiquidMetalCard` or equivalent), the same `MaturityBadge` style for status indicators, and consistent heading/body typography from the shared design system. No jarring layout shifts, inconsistent border treatments, or different background atmospheres between old and new surface focus states.
Evidence: Focus each new graph node in `GraphShell` and compare rendered output to an existing surface (e.g., roadmap). Verify: (1) card wrappers use `LiquidMetalCard` or the project's standard card component, (2) maturity status uses `<MaturityBadge>` with the same size/color tokens, (3) heading hierarchy (`h2`/`h3` levels) matches existing patterns, (4) CSS transitions on focus/unfocus are identical to existing nodes (no missing or different `transition` properties). Fail if any new surface uses an ad-hoc card style, inline status badges, or lacks the shared atmosphere overlay.

### VAL-CROSS-018: Graph edges between new and existing nodes work bidirectionally
Every `related` edge declared in a new `GraphNode` entry must be bidirectional: if node A lists node B in `related`, then node B must also list node A. Clicking a related-node traversal control from any existing node that edges into a new node must update `focusedNodeId` to the new node (and vice versa). No dead edges or one-way references.
Evidence: For each new node in `GRAPH_NODES`, verify `related` IDs all resolve via `getGraphNodeById`. For each related ID, confirm the reverse node's `related` array includes the new node's ID. In the rendered `GraphShell`, click each related-node pill/button and confirm the URL hash updates to `#graph-<targetId>` and the focused surface renders. Fail if any `related` ID is unresolvable, any edge is one-directional, or any click fails to navigate.

### VAL-CROSS-019: Mobile responsiveness for new surfaces
All newly added graph-node surfaces must render correctly on mobile viewports (≤ 640px width). Content must not overflow horizontally, interactive elements must have touch-friendly tap targets (≥ 44px), and the `MobileNav` component must include entries for new surfaces. The graph overview on mobile must show new nodes without requiring horizontal scroll.
Evidence: Render each new surface at 375px viewport width. Verify: (1) no horizontal scrollbar appears, (2) all buttons and interactive elements pass a minimum 44×44px hit-area check, (3) text remains readable without pinch-zoom (font-size ≥ 14px for body), (4) `MobileNav` lists new nodes with correct labels matching `GraphNode.label`, (5) the mobile graph overview renders all ~13 nodes within the viewport width. Fail if any surface overflows, has sub-44px tap targets, or is missing from `MobileNav`.

### VAL-CROSS-020: New surfaces carry proper maturity labels consistent with existing surfaces
Every new graph node must declare a `dominantStatus` value from the canonical `MaturityStatus` union (`"live" | "in_progress" | "planned"`). The rendered surface must display a `<MaturityBadge status={dominantStatus} />` that uses the same label text and color tokens as existing surfaces. If a new surface contains sub-components with mixed maturity, each must carry its own `MaturityBadge` — no unlabeled sections.
Evidence: For each new `GraphNode`, verify `dominantStatus` is a valid `MaturityStatus` value. Render the focused surface and confirm at least one `MaturityBadge` is present with matching status. Cross-reference against `MATURITY_LABELS` and `MATURITY_DESCRIPTIONS` from the type system to ensure label text consistency. Inspect that badge color classes match existing badge instances (e.g., `live` → green dot, `in_progress` → amber dot, `planned` → grey dot). Fail if any new surface omits a maturity badge or uses a non-canonical status string.

---

### VAL-POLISH-001: No visual regressions on existing surfaces after content expansion
All 9 pre-existing graph-node surfaces (what-is-better, proof, live-now, roadmap, tokenomics, architecture, valuation, evidence, risks) must render identically to their pre-expansion state. No layout shifts, missing components, broken imports, changed typography, or altered color tokens. The brand band, `BetterLogotype`, `CaveatFrame`, `ProofModule`, `LiquidMetalCard` instances, and atmosphere overlays must all remain intact.
Evidence: Render `<Home />` and focus each of the 9 existing graph nodes. Compare rendered DOM structure against a baseline snapshot (or verify key `data-testid` elements remain present and correctly styled). Run the existing test suite — all existing tests in `src/app/__tests__/`, `src/components/__tests__/`, and `src/content/__tests__/` must pass with zero regressions. Fail if any existing test fails or if visual inspection reveals changed layout, missing elements, or altered styling on any existing surface.

### VAL-POLISH-002: New surfaces are lazy-loaded and excluded from the initial bundle
All newly added surface components must be code-split via `React.lazy()`, Next.js `dynamic()`, or equivalent lazy-loading boundaries. They must NOT be included in the initial JavaScript bundle loaded on first page visit. Only the graph shell chrome and the default-focus surface should be in the critical path.
Evidence: Run `next build` and inspect the build output's chunk manifest. Verify that new surface components appear in separate chunks (not in the `page.js` or `layout.js` entry chunks). Alternatively, check that new surface imports use `dynamic(() => import(...))` or `React.lazy(() => import(...))` patterns — direct top-level `import` of new surface components from `page.tsx` or `layout.tsx` is a fail. Confirm that `LazyProofModule`-style wrapper patterns are used for new surfaces. The total initial JS bundle size must not increase by more than 5 KB gzipped from new surface additions.

### VAL-POLISH-003: All changes committed and GitHub push successful
Every file added or modified during the content-expansion and shadcn-migration milestones must be committed to the repository with meaningful commit messages. `git status` must show a clean working tree (no untracked or unstaged files related to the milestone). The branch must be pushed to the remote origin successfully.
Evidence: Run `git status` — working tree must be clean. Run `git log --oneline -20` and verify commits cover the new surfaces, shadcn migration, and polish changes. Confirm `git push` completes without errors (no rejected refs, no authentication failures). If a pull request is required, verify `gh pr list` shows the relevant PR. Fail if any milestone-related file is uncommitted, if the push is rejected, or if the remote branch is behind.

### VAL-POLISH-004: Production build succeeds with zero warnings and zero errors
`next build` must complete with exit code 0, producing no TypeScript errors, no ESLint errors, and no build warnings. The build output must show all pages compiled successfully with no "Failed to compile" or "warning" lines. Additionally, `next lint` (or `eslint`) must pass cleanly across all source files.
Evidence: Run `next build 2>&1` and capture full output. Verify: (1) exit code is 0, (2) zero lines matching `/error/i` in the build output (excluding intentional error-boundary component names), (3) zero lines matching `/warning/i` (excluding known framework info messages like "Compiled successfully"), (4) all routes listed in the build manifest show valid sizes. Run the project's lint command and confirm zero violations. Run `npx tsc --noEmit` and confirm zero type errors. Fail if any error, warning, or type diagnostic is present.
